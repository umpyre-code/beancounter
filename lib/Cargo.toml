[package]
name = "beancounter-lib"
version = "0.1.0"
authors = ["Brenden Matthews <brenden@brndn.io>"]
edition = "2018"

[dependencies]
prost = "0.5"
bytes = "0.4"
tower-grpc = "0.1"
tower-hyper = "0.1"

[build-dependencies]
tower-grpc-build = { version = "0.1", features = ["tower-hyper"] }
